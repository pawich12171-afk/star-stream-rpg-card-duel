{
  "buildCommand": "node scripts/fix-persistence-before-build.mjs && node scripts/fix-status-save.mjs && vite build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
